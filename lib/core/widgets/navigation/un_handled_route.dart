import 'package:flutter/material.dart';

class UnHandledRoute extends StatelessWidget {
  const UnHandledRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("UnHandledRoute"),
      ),
    );
  }
}
